import data from '../../constants/data'

const initialState = data

export default function pharses (state = initialState, action='') {
    switch (action.type) {
        default:
            return state
    }
}